# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git - it contains sensitive information

# =============================================================================
# DATABASE CONFIGURATION  
# =============================================================================
# PostgreSQL database connection string (supports any PostgreSQL provider)
# For local development with Supabase: postgresql://postgres:postgres@localhost:54322/postgres
# For production: Use your hosted PostgreSQL URL
DATABASE_URL=postgresql://user:password@localhost:5432/database

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================
# The base URL of your application
NEXTAUTH_URL=http://localhost:3000

# Generate a secure secret for production (required for JWT signing)
# Run: openssl rand -base64 32
NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32

# =============================================================================
# OAUTH PROVIDER CONFIGURATION
# =============================================================================
# See setupGuides/githubAuthInstructions.md for setup guide
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# See setupGuides/googleAuthInstruciton.md for setup guide  
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Set to 'production' when deploying
NODE_ENV=development

# PADDLE CONFIGURATION
PADDLE_SECRET_TOKEN=your_paddle_secret_token
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=your_next_public_paddle_client_token

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 🔒 CRITICAL SECURITY RULES:
# 
# 1. NEXT_PUBLIC_* variables are exposed to the browser (client-side)
#    - Only use for non-sensitive configuration
#    - These can be seen by anyone visiting your site
# 
# 2. Variables without NEXT_PUBLIC_ are server-side only
#    - Never expose secrets like DATABASE_URL to the client
#    - These are used in API routes and server-side code only
# 
# 3. For production deployment:
#    - Generate a strong NEXTAUTH_SECRET: openssl rand -base64 32
#    - Set NODE_ENV=production
#    - Use your production database URL
#    - Configure OAuth providers with production redirect URLs
# 
# 4. Never commit .env.local or .env.production to version control
#    - Add these files to .gitignore (already done)
#    - Use your deployment platform's environment variable settings
# 
# =============================================================================

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 🔒 CRITICAL SECURITY RULES:
# 
# 1. NEXT_PUBLIC_* variables are exposed to the browser (client-side)
#    - Only use for non-sensitive configuration
#    - These can be seen by anyone visiting your site
# 
# 2. Variables without NEXT_PUBLIC_ are server-side only
#    - Never expose secrets like SUPABASE_SERVICE_ROLE_KEY to the client
#    - These are used in API routes and server-side code only
# 
# 3. For production deployment:
#    - Generate a strong NEXTAUTH_SECRET: openssl rand -base64 32
#    - Set NODE_ENV=production
#    - Use your production Supabase project URL and keys
#    - Configure OAuth providers with production redirect URLs
# 
# 4. Never commit .env.local or .env.production to version control
#    - Add these files to .gitignore (already done)
#    - Use your deployment platform's environment variable settings
# 
# =============================================================================
